name: Deploy 

on:
  push:
    branches:
      - main


jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Clone Repo
        run: | echo "Cloned Repo"

  testing_types:
     runs-on: ubuntu-latest
     needs:
       - checkout
     steps:
       - name: Testing
       run: | echo "DONE"




       

